1.sum:求和函数
    ·s=sum(v) 求向量v中元素的和
    ·s=sum(A,1)或s=sum(A) 求矩阵A中每列的和，返回成1个行向量
    ·s=sum(A,2) 求矩阵A中每行的和，返回成1个列向量
2.mean:求平均值
    ·s=mean(v) 求向量v中元素的平均值
    ·s=mean(A,1)或s=mean(A) 求矩阵A中每列的平均值，返回成1个行向量
    ·s=mean(A,2) 求矩阵A中每行的平均值，返回成1个列向量
3.max:求最大值   min:求最小值
    ·[v, I]=max(x) 
        ·如果x为向量，v为向量中的最大元素，I为最大元素在x中的下标，即索引
        ·如果x为矩阵，v为每列的最大元素组成的行向量，I则为每列最大元素的行下标组成的向量
    ·[v, I]=min(x)
        ·如果x为向量，v为向量中的最小元素，I为最小元素在x中的下标
        ·如果x为矩阵，v为每列的最小元素组成的行向量，I则为每列最小元素的行下标组成的向量
4.lenth与size
    ·length(x)
        ·如果x为向量，则返回x的元素个数
        ·如果x为矩阵，则返回矩阵x行数和列数的最大值
    ·size(x)
        ·如果x为向量、矩阵，则返回行数、列数组成的行向量
        ·disp(size(M,1)) 返回矩阵M行数
        ·disp(size(M,2)) 返回矩阵M列数
5.sort:排序
    ·[B, I]=sort(v) 对向量v中元素排序，B为按递增排序后的元素；I为排序后数组B中的元素在原数组v中的位置下标
    ·当v是矩阵时，对列分别排序
    ·sort(a, 'ascend') 按升序排列
    ·sort(a, 'descend') 按降序排列
6.find:查找
    ·find函数用于查找数组中的非零元素位置
    ·idx=find(a>=6) 结合逻辑表达式可以返回所需要元素的位置下标
7.fix:取整
    fix(number)
8.sqrt:取更号
9.mod:求余数
    b = mod(a, m)   % b返回a除以m的余数