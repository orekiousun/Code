1.rand(n):生成一个随机的n*n的矩阵
2.diag:求矩阵A的对角矩阵
    diag(diag(A))   % 只用一次diag(a)返回的是矩阵a的对角线组成的列向量
3.triu:获取上三角矩阵
    triu(A)
4.tril:获取下三角矩阵
    tril(A)
5.解线性方程组Ax=b
    ·A\b   # 表示a左除以b
    inv(A)*b   % inv(a)表示a的逆
6.翻转矩阵
    b = b'  %取b的转置
    fliplr(a)  %左右翻转
    flipud(a)  %上下翻转
7.将矩阵排成一列
    a(:)
8.求行列式
    det(a)
9.求矩阵特征值，特征向量
    [V, D]=eig(A)   %输出特征值和对应特征向量，v为特征向量，d为特征值，
    lambda=eig(A)   %计算出A的特征值构成的列向量
    实对称矩阵的特征值一定是实数